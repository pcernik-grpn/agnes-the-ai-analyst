# Maps

`agnes-wayfinder` output for large, foggy efforts — a map of decision tickets
resolved one per session until a plan can be written. Each effort gets its own
`<effort-slug>/` subdirectory (`map.md` + `issues/NN-<slug>.md`); see the
`agnes-wayfinder` skill for the format.
