### Internal
- A clean Devin verdict now supplies the approving review that lets an organization member's pull request enter `main`'s merge queue, and is dismissed again when a later Devin review lists issues (`devin-clean-approves.yml`). The queue evaluates the one-approval rule as itself, so ruleset bypass no longer let a member queue an unreviewed PR; outside collaborators' PRs and the daily release-cut PR still need a human approval (#2295).
