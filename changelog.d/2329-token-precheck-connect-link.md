### Changed
- **The install prompt's "not signed in" fallback now points to
  `/how-it-works#connect`, not `/home`.** The CLI tab there already has the
  same "Set up a new Claude Code" one-click button (mints a token, copies
  the setup script) that used to live only on `/home` step 4.
