### Changed
- **Step 4 of the install prompt now suggests a first data question**, not
  just a recap of what got installed — matches the nudge the newer
  `/llms.txt` setup-code flow already gives ("What data can I access
  through {brand}? ... suggest three useful questions").
