### Fixed
- The thumbs on a chat answer no longer wait for a hover. They live in the
  same row as the timestamp and the copy button, which fades in when the
  pointer is over the message — so on a touch screen they never appeared at
  all, and on a desktop only by accident. An answer that can be rated now
  shows that row from the start.
