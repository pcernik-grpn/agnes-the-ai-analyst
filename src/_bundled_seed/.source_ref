# Provenance of the bundled seed snapshot in this wheel.
# Generated by scripts/sync_bundled_seed.sh on 2026-08-19T14:37:00Z.
# Edit by re-running the script, NOT by hand — the CI guard at
# .github/workflows/check-bundled-seed.yml verifies the SHA below is a real
# commit at source_url.
source_url=https://github.com/keboola/agnes-infra-template
source_sha=a23307e275f3f99c94370da36d8c15b6c8d86498
source_branch=main
# DELIBERATE DIVERGENCE from upstream@source_sha (the CI content diff is
# warning-only by design; this note keeps the guard honest for readers and
# for any future flip of that step to a hard failure):
#   - install-prompt/template.md.tmpl: rewritten to the thin self-contained
#     shape (this repo's docs/seed-repo-contract.md §5); upstream still
#     ships the pre-thin version. Upstreaming is a noted follow-up.
#   - connector-gws SKILL.md: upstream's update NOT taken — it carries
#     TLS-off phrasing tests/test_install_prompt_banned_phrases.py refuses.
